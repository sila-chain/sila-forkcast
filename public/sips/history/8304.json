{
  "eipId": 8304,
  "commits": [
    {
      "sha": "991d932f52a56477753cd9f62114b842cd77275c",
      "date": "2026-09-08T09:19:08Z",
      "message": "Update SIP-8304: Update sip-8304.md",
      "author": "zsfelfoldi",
      "prNumber": null,
      "patch": "@@ -13,7 +13,7 @@ requires: 4788\n \n ## Abstract\n \n-Store the root hashes of fixed-depth binary tree structures called \"index tables\" in the storage of a system contract as part of the block processing logic. A new table is generated for each block, then merged into bigger tables indexing multiple blocks. The proposed design prioritizes minimalization of index processing costs during block processing. The SIP has no effect on the bloom filters stored in block headers.\n+Store the root hashes of binary tree hashed lists called \"index tables\" in the storage of a system contract as part of the block processing logic. A new table is generated for each block, then merged into bigger tables indexing multiple blocks. The proposed design prioritizes minimalization of index processing costs during block processing. The SIP has no effect on the bloom filters stored in block headers.\n \n ## Motivation\n \n@@ -68,6 +68,14 @@ The following entry types are defined:\n \n Entries consist of an entry type id, the searchable content and the position information. In case of transaction entries the _cumulative log count_ (the total number of logs in the block before the given transaction) is also added. The _log index_ of address/topic entry types is relative to the transaction beginning. All numbers are encoded as big-endian which ensures correct ordering based on the binary representation.\n \n+##### SIP-8141 extension\n+\n+If [SIP-8141](./sip-8141.md) is activated then a two-byte frame index is added between the transaction and log index. Note that encoding position info with a separate frame index will be most useful if receipt encoding is changed to SSZ but adding it right away is easier than updating it later. While receipt format does not directly affect this protocol update, a tree-hashed receipt encoding would allow a Merkle inclusion proof of individual logs based on tx/frame/log position info, making log query proofs significantly more efficient. Since index entries provide the exclusion proof proving the completeness of the inclusion set, their position encoding has to match the inclusion proof position information so that they can be matched against each other.\n+\n+##### Future extensions\n+\n+Future updates might require adding new entry types or changing the position encoding if the block structure changes. This should be possible without reindexing pre-fork blocks, which also means that entries of pre-fork and post-fork tables would be merged together. The only invariant that needs to be maintained during such updates is that entries are ordered lexicographically according to the same binary encoding that is also used for hashing. Since index entries are prefixed by entry type, new entry types describing new kinds of events can be added without any issues. Existing entry encoding can also be changed or extended after the block number field of the position info while still maintaining correct position ordering of entries with identical type and value.\n+\n #### Indexing rules\n \n Transaction and log address/topic indexing is straightforward, for each transaction and each log event in the indexed block the corresponding index entries are added. Block entries are added with a one-block delay; for each block `N` the block entry of the parent block `N-1` is added. For the genesis block no block entry is added. A multi-block merged table covering the block range `first_block` to `first_block + table_size - 1` contains the block entries from `first_block - 1` to `first_block + table_size - 2`. This one-block offset is due to the fact that the single-block table generated from the actually processed block is referenced in the same block, before the block hash can be determined.\n@@ -365,7 +373,6 @@ The ring buffer of each table level is 1024 tables long. This means that approxi\n \n This SIP introduces backwards incompatible changes to the block validation rule set, but neither of these changes break anything related to current user activity and experience.\n \n-\n ## Security Considerations\n \n Having contracts (system or otherwise) with hot update paths (branches) poses a risk of \"branch\" poisoning attacks where attacker could sprinkle trivial amounts of sil around these hot paths (branches). But it has been deemed that cost of attack would escalate significantly to cause any meaningful slow down of state root updates.",
      "additions": 9,
      "deletions": 2
    },
    {
      "sha": "a9b074cffb5bea63b201ee5fabf19553dbfe483a",
      "date": "2026-07-14T16:43:50Z",
      "message": "Add SIP: Trustless log and transaction index",
      "author": "zsfelfoldi",
      "prNumber": null,
      "additions": 375,
      "deletions": 0
    }
  ]
}
