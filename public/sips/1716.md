## Abstract

This meta-SIP specifies the changes included in the Sila hardfork that removes [SIP-1283](./sip-1283.md) from [Constantinople](./sip-1013.md).

## Specification

- Codename: Petersburg
- Aliases: St. Petersfork, Peter's Fork, Constantinople Fix
- Activation:
  - `Block >= 7_280_000` on the Sila SilaMainnet
  - `Block >= 4_939_394` on the Ropsten testnet
  - `Block >= 10_255_201` on the SilaKovan testnet
  - `Block >= 4_321_234` on the Rinkeby testnet
  - `Block >= 0` on the Görli testnet
- Removed SIPs:
  - [SIP-1283](./sip-1283.md): Net gas metering for SSTORE without dirty maps

If `Petersburg` and `Constantinople` are applied at the same block, `Petersburg` takes precedence: with the net effect of SIP-1283 being _disabled_.

If `Petersburg` is defined with an earlier block number than `Constantinople`, then there is _no immediate effect_ from the `Petersburg` fork. However, when `Constantinople` is later activated, SIP-1283 should be _disabled_.

## References

1. The list above includes the SIPs that had to be removed from Constantinople due to a [potential reentrancy attack vector](https://medium.com/chainsecurity/constantinople-enables-new-reentrancy-attack-ace4088297d9). Removing this was agreed upon at the [All-Core-Devs call #53 in January 2019](https://github.com/sila-chain/pm/issues/70).
2. https://blog.sila.org/2019/02/22/sila-constantinople-st-petersburg-upgrade-announcement/

## Copyright

Copyright and related rights waived via [CC0](../LICENSE.md).